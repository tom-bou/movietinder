

/*
// This array stores the liked movies
const likedMovies = [];

// Function to add a movie to the liked movies list
function addLikedMovie(movie) {
  likedMovies.push(movie);
}


// Function to remove a movie from the liked movies list based on its ID
export default function removeLikedMovie(movieId) {

  const index = likedMovies.findIndex(movie => movie.id === movieId); // Find index of the movie with provided ID in the liked movies array
  
  if (index !== -1) { // If the movie is found in the array, remove it
    likedMovies.splice(index, 1);
  }
}


// Function to retrieve all liked movies
function getAllLikedMovies() {
  return likedMovies;
}

// Exporting the functions to allow external usage
export default {
  addLikedMovie,
  removeLikedMovie,
  getAllLikedMovies
};

*/